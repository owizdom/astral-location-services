# =============================================================================
# EIGENCOMPUTE PRODUCTION CONFIGURATION
# =============================================================================
# This is a REFERENCE file showing what EigenCompute provides/expects.
#
# IMPORTANT: You do NOT provide the MNEMONIC yourself.
# EigenCompute generates it via KMS and injects it - only the TEE can decrypt it.
# =============================================================================

# -----------------------------------------------------------------------------
# SIGNER (PROVIDED BY EIGENCOMPUTE - DO NOT SET)
# -----------------------------------------------------------------------------
# EigenCompute automatically provides MNEMONIC via their KMS.
# It's encrypted and only decryptable inside the TEE.
# Your app just reads process.env.MNEMONIC - you never see the actual value.
#
# MNEMONIC=<provided by EigenCompute KMS, not by you>

# -----------------------------------------------------------------------------
# DATABASE (internal to container)
# -----------------------------------------------------------------------------
# PostGIS runs inside the container - no external database needed
DATABASE_URL=postgresql://astral:astral@localhost:5432/astral
POSTGRES_USER=astral
POSTGRES_PASSWORD=astral
POSTGRES_DB=astral

# -----------------------------------------------------------------------------
# CHAIN
# -----------------------------------------------------------------------------
CHAIN_ID=84532  # Base Sepolia for now, 8453 for mainnet later

# -----------------------------------------------------------------------------
# SCHEMA UIDs (registered on Base Sepolia)
# -----------------------------------------------------------------------------
# These are the default schema UIDs for policy attestations.
# Clients can override with custom schemas (e.g., for resolver contracts).
NUMERIC_SCHEMA_UID=0xc2b013ecb68d59b28f5d301203ec630335d97c37b400b16b359db6972572e02a
BOOLEAN_SCHEMA_UID=0x4958625091a773dcfb37a1c33099a378f32a975a7fb61f33d53c4be7589898f5

# -----------------------------------------------------------------------------
# SERVER
# -----------------------------------------------------------------------------
PORT=3000
NODE_ENV=production
ALLOWED_ORIGINS=https://astral.global,https://app.astral.global

# -----------------------------------------------------------------------------
# TLS (custom domain)
# -----------------------------------------------------------------------------
DOMAIN=compute.astral.global
APP_PORT=3000
ACME_STAGING=false  # true for testing, false for real certs
